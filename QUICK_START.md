# Quick Start Guide

A quick reference for getting started with this React project.

## 🚀 First Time Setup

1. **Install Node.js** (if not already installed)
   - Download from [nodejs.org](https://nodejs.org/)
   - This includes `npm` (Node Package Manager)

2. **Install project dependencies**
   ```bash
   npm install
   ```

3. **Start the development server**
   ```bash
   npm run dev
   ```

4. **Open your browser**
   - Go to the URL shown in the terminal (usually `http://localhost:5173`)
   - You should see your React app!

## 📝 Common Commands

| Command | What it does |
|---------|-------------|
| `npm install` | Install all dependencies |
| `npm run dev` | Start development server (use this while coding) |
| `npm run build` | Create production build |
| `npm run preview` | Preview production build |
| `npm run lint` | Check code for errors |

## 🗂️ Where to Find Things

| File/Folder | What's inside |
|-------------|---------------|
| `src/App.tsx` | Main component - edit this to change your app |
| `src/components/` | Reusable components folder |
| `src/App.css` | Styles for App component |
| `src/index.css` | Global styles (affects entire app) |
| `package.json` | Project dependencies and scripts |
| `README.md` | Complete documentation and learning guide |

## 📖 What to Read Next

1. **README.md** - Complete guide to React concepts and project structure
2. **src/App.tsx** - Read the comments to understand how React works
3. **src/components/ExampleComponent.tsx** - See how to create new components

## 🎯 Your First Task

Try modifying `src/App.tsx`:
- Change the text in the header
- Modify the button text
- Change colors in `src/App.css`

Save the file and watch it update automatically in your browser!

## ❓ Need Help?

- Check `README.md` for detailed explanations
- Visit [react.dev](https://react.dev) for official React docs
- Look at code comments - they explain everything

Happy coding! 🎉
